Vagrant.configure("2") do |config|
  config.vm.box = "centos/7"
  config.vm.hostname = "dev-vagrant"
  config.vm.provider "virtualbox" do |b|
        b.memory = "2222"
        b.cpus = 2
	b.name = "dev-vagrant"
end
end
